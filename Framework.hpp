#pragma once

// SDL Libraries
#include <SDL.h>
#include <SDL_image.h>

// Standard Libraries
#include <array>

// QUICK DEBUG
#include <iostream>

using std::array;
using size = size_t;
using ErrorCode = int;

// GLOBAL VARIABLES
const ErrorCode success = 0, failure = 1;